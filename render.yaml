services:
  - type: web
    name: petezz-wechat-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: WECHAT_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
